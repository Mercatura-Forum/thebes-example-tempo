# build / deploy artifacts
dist/
asset_canister.wasm

# third-party raw model source (re-textured GLB lives in assets/)
# — but our original rework pipeline under src-model/tools/ IS tracked
src-model/*
!src-model/tools/
src-model/tools/out/
*.rar

node_modules/
.DS_Store

# donor splash mesh — input to src-model/tools/build_splash.py, not redistributed
/water_splash.glb

# local scratch (screenshots etc.)
/image.png

# motoko compile output
build/

# editor / assistant scratch
.vscode/
.idea/
.claude/
.cursor/

# deploy tool state
.thebes/

# mops package cache + lock (machine-local paths)
motoko/.mops/
motoko/mops.lock
